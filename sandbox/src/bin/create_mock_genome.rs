// Create mock HLA diploid genome for synthetic datasets.
// ```
// cargo run --release --bin create_mock_genome -- ${HG38}.fasta > ${SYNTHETIC}
// ```
fn main() -> std::io::Result<()> {
    Ok(())
}
